# 三进制计算在具身智能中的结构性优势

## ——从非光滑动力学与部分可观测性看表示层的亲和性

**Structural Advantages of Ternary Computing for Embodied Intelligence:**
**Representational Affinity in Non-smooth Dynamics and Partial Observability**

---

**作者**：lbylzk8
**日期**：2026 年 9 月
**关键词**：平衡三进制；具身智能；非光滑动力学；部分可观测性；三值逻辑；量化；世界模型

---

## 摘要

三进制计算在人工智能领域的既有论证几乎都建立在**效率叙事**之上：三值权重量化消除乘法、降低存储与带宽开销，最终体现为 BitNet b1.58 等大语言模型的能效提升。本文指出，将同一套论证平移至具身智能是**不充分的**——因为具身智能的核心瓶颈不是算力，而是**建模**。

本文提出一个与效率叙事正交的论证：三进制对具身智能的价值主要不在"更省"，而在**表示层的结构同构**。我们首先将具身智能的建模难点分解为四层（状态表征、动力学建模、不确定性建模、任务建模），并诊断出二进制表示在其中三个子问题上存在系统性失配：（1）符号-幅值失配，即 $\mathrm{sgn}$ 函数与二值数码集不同构；（2）认识态坍塌，即"未知"无法作为一等值存在；（3）有偏舍入，即补码截断相对零点不对称，导致长时程 rollout 中出现 $O(T)$ 的系统性漂移。

随后我们论证平衡三进制的数码集 $\{-1, 0, +1\}$ 在上述三处构成天然契合，并给出定量分析：在每步量化偏差为 $\mu$、标准差为 $\sigma$ 时，有偏累积漂移在 $T \gtrsim (\sigma/\mu)^2$ 步后主导总误差——以典型 ULP 尺度估算，该阈值仅为数步量级。

为避免本文沦为一篇立场性宣言，我们明确给出三个**可证伪假设**（长时程漂移、未知态安全性、接触任务颤振）及配套实验设计，其中关键消融在于区分"三值量化（运行于二进制硬件）"与"原生三进制运算"两种模式。最后，我们给出三个时间尺度的硬件实现路线图，并逐条反驳四种可能的主要质疑，同时明确列出本文**不主张**的结论（三进制不提升样本效率、不解决 sim-to-real 差距、原生硬件短期不可量产）。

**本文的核心主张是**：在语言模型领域，三进制是一个省钱工程技巧；而在具身智能领域，它与非光滑动力学、部分可观测性和符号动作共享数学结构。这一差别使得具身智能成为三进制优势**最根本**的应用方向。

---

## 1 引言

### 1.1 两种叙事

三进制计算的历史论证有一条清晰的谱系。从信息论上，基数为 $r$ 的表示法在"基数 × 位数"度量下的最优值是 $e \approx 2.718$，而整数基数中 $3$ 最接近 $e$；从工程上，平衡三进制免除了符号位、统一了加减法、使舍入对称。这条论证在 1958 年的 Setun 计算机上得到过工程验证，在 2024 年的 BitNet b1.58 中则以量化形式复活。

然而，当我们把目光从语言模型转向**具身智能**（embodied intelligence）时，上述论证的适用范围需要重新审视。

原因在于瓶颈的转移。语言模型的瓶颈确实是算力与带宽：模型规模以千亿参数计，推理成本是核心约束。而具身智能的瓶颈是**建模**——如何在部分可观测、接触丰富、非光滑的真实物理世界中，构建一个可预测、可规划、可安全执行的内部模型。算力在这个问题上是二级约束：一台四足机器人或机械臂的机载算力预算是瓦特级的，而它面对的状态空间复杂度并不因此降低。

因此，一个诚实的追问是：**三进制对具身智能的价值，是否仅仅是"在更小的算力预算下做同样的事"？**

本文的回答是否定的，并尝试给出一个更强的论证。

### 1.2 本文的论证策略

我们采取如下策略：

1. **承认边界**：明确区分"三进制能帮上忙的建模层次"与"三进制无关的建模层次"。后者包括样本效率、奖励设定、仿真到现实的差距（sim-to-real gap）。在这些问题上本文**不主张**任何优势。
2. **定位机制**：在表示层（representation layer），论证三进制的数码集与具身智能中三类关键结构之间存在**同构或同态**关系，这是一种比"效率优化"更强的关系。
3. **给出可否证的预测**：任何"归纳偏置更优"的主张若不可证伪，就只是修辞。本文为每个契合点给出可测量的假设与对照实验设计。
4. **明确消融维度**：区分"三值量化"与"原生三进制"，这是判断优势来源的关键——若二者表现相同，则优势仅来自实现效率；若仅有原生三进制优于全精度基线，则优势来自表示代数本身。

### 1.3 贡献

- 给出具身智能建模难点的四层分解，并诊断二进制表示在三个子问题上的系统性失配（第 2 节）。
- 论证平衡三进制与这三处失配的结构性契合，并给出舍入漂移的定量分析（第 3 节）。
- 提出三个可证伪假设与完整实验设计，含关键消融方案（第 4 节）。
- 给出三时间尺度的硬件路线图与成本-收益分析（第 5 节）。
- 逐条反驳四种主要质疑，并明确列出不成立的过度声称（第 6 节）。

### 1.4 与既有工作的关系

本文与三值量化文献（TWN、TTQ、BitNet b1.58）的关系是**互补而非重复**。既有工作回答的问题是"如何在二进制硬件上逼近全精度性能"；本文回答的问题是"在具身智能中，哪些建模子问题与三值结构天然同构"。前者是数值逼近问题，后者是表示选择问题。二者可以独立成立，也可以相互叠加。

---

## 2 具身智能建模难点的形式化

### 2.1 标准模型与四层分解

具身智能的决策问题通常形式化为部分可观测马尔可夫决策过程（POMDP）：

$$\mathcal{M} = \langle \mathcal{S}, \mathcal{A}, T, R, \Omega, O, \gamma \rangle$$

其中 $T(s' \mid s, a)$ 为转移函数，$O(o \mid s', a)$ 为观测函数，$R(s, a)$ 为奖励函数。信念状态为

$$b_t(s) = P(s \mid h_t), \qquad h_t = (o_1, a_1, \ldots, o_t)$$

"建模"由此分解为四个层次：

| 层次 | 建模对象 | 数学形式 | 数据/仿真敏感性 |
| --- | --- | --- | --- |
| L1 状态表征 | 高维连续、部分可观测 | $b_t(s)$ 或隐变量 $z_t$ | 中 |
| L2 动力学建模 | 接触、摩擦、碰撞 | $\hat{T}(s' \mid s, a)$ | **高** |
| L3 不确定性建模 | 遮挡、传感器失效 | $\mathrm{Var}[b_t]$、认识不确定性 | 中 |
| L4 任务建模 | 稀疏奖励、偏好、约束 | $\hat{R}$、安全谓词 $\phi$ | **高** |

**本文的主张范围**：三进制的结构性优势作用于 L1、L3 的表示形式，以及 L2 中**非光滑**部分的表示。对于 L2/L4 中由数据稀缺和仿真差距引起的问题，本文不主张任何优势——这是必须首先划清的边界。

### 2.2 三类"符号敏感"结构

我们识别出具身智能建模中三类对**符号（sign）与有序性**高度敏感的结构。称其"符号敏感"，是指这些结构的数学表达中，$0$ 与 $\pm 1$ 扮演不可替代的角色，且零点附近的对称性具有物理意义。

#### 2.2.1 结构 A：非光滑与切换动力学

真实机器人的动力学在接触瞬间是不连续的。库仑摩擦力与滑模控制律的经典形式为

$$F_f = -\mu N \,\mathrm{sgn}(v), \qquad u = -k\,\mathrm{sgn}(\sigma)$$

其中 $\mathrm{sgn}(x) \in \{-1, 0, +1\}$。继电器、死区、滞环、饱和等基本非线性（归一化后）也都以此为码域：

$$\mathcal{N}_{\text{sign}} = \{\mathrm{sgn},\ \mathrm{deadzone},\ \mathrm{relay},\ \mathrm{hysteresis},\ \mathrm{sat}_{\pm 1}\}$$

接触丰富操作（contact-rich manipulation）被公认为具身智能最难的方向之一；其困难本质就是**非光滑性**：黏滞（stick）与滑动（slip）之间的切换使梯度不存在，使仿真必须使用隐式时间步进与线性互补问题（LCP）求解。

#### 2.2.2 结构 B：部分可观测与认识不确定性

POMDP 的"部分"意味着智能体必须区分三种认识态：

- **确认成立**（$b(\phi) = 1$）
- **确认不成立**（$b(\phi) = 0$）
- **未知**（$0 < b(\phi) < 1$，或由于模型缺失而无法赋值）

对于安全谓词 $\phi$（"前方无障碍"、"夹爪已夹稳"），第三种状态既不能归入安全也不能归入不安全，而必须有独立的保守回退语义。这一需求在形式化方法中已由三值逻辑与三支决策理论充分刻画。

值得注意的是，工程实践**独立地重新发明了三值表示**：

| 系统 | 状态数 | 三值语义 |
| --- | --- | --- |
| ROS / Nav2 costmap | 3 | free / occupied / unknown |
| OctoMap 占据栅格 | 3 | free / occupied / unknown |
| 机器人安全监控（SIL 2/3） | 3 | safe / unsafe / indeterminate |
| 三支决策框架 | 3 | accept / reject / defer |

也就是说，"未知"作为一等值的必要性在工程实践中已被广泛承认，只是缺少与之匹配的**原生代数**。

#### 2.2.3 结构 C：对称性与长期一致性

世界模型的想象力 rollout 需要多步迭代。设每步的量化误差为 $\epsilon_i$，则 $T$ 步后的累积误差为

$$E_T = \sum_{i=1}^{T} \epsilon_i$$

若 $\epsilon_i$ 独立同分布，均值为 $\mu$，标准差为 $\sigma$，则

$$\mathbb{E}[E_T] = T\mu, \qquad \mathrm{Std}[E_T] = \sigma\sqrt{T}$$

偏差项随 $T$ 线性增长，随机项仅随 $\sqrt{T}$ 增长。二者主导权的交叉点为

$$T|\mu| \gtrsim \sigma\sqrt{T} \quad\Longrightarrow\quad T \gtrsim \left(\frac{\sigma}{\mu}\right)^{2}$$

**取典型量化场景**：若 $\sigma \approx 1$ ULP、$\mu \approx 0.5$ ULP，则 $T \gtrsim 4$。也就是说，**超过约 4 步的 rollout 后，舍入偏差就压倒了随机误差**。而具身智能的世界模型 rollout 通常为 $10$–$10^{3}$ 步。这意味着舍入的**有偏性**——而非其方差——是长时程预测精度的主要敌人。

### 2.3 二进制表示的失配诊断

针对上述三类结构，标准二进制表示存在三处系统性失配。

#### 2.3.1 失配一：删号-幅值失配（对应结构 A）

设 $n$ 位补码整数。提取符号需要：检查最高位、判定是否为零（零有唯一表示，但判定需要整字比较）、对负数做取反加一的语义解释。

- **符号提取代价**：$\Theta(n)$ 位读取 + 分支，或依赖专用标志位。
- **零点地位**：$0$ 在补码中与其它值无结构区别；"黏滞态"（$v = 0$）不是一等值，而是值域中一个普通点。
- **码域不同构**：$\mathrm{sgn}$ 的码域是 $\{-1, 0, +1\}$（3 个元素），二进制数码集是 $\{0, 1\}$（2 个元素）。二者之间不存在保结构的双射。

#### 2.3.2 失配二：认识态坍塌（对应结构 B）

在二值框架下表示"未知"必须引入**值-有效性对** $(v, m) \in \{0,1\}^2$，其中 $m = 0$ 表示未知。由此产生三个问题：

1. **位宽翻倍**：每个占据栅格单元从 1 bit 变为 2 bit，且 4 个状态中恰有 1 个不可达（$m=0$ 时 $v$ 无意义）。
2. **代数不闭合**：$\mathrm{AND}$、$\mathrm{OR}$ 需要额外的掩码规则。例如 $m = 0$ 时输出应强制为未知，该规则无法由 $\wedge$、$\vee$ 导出，必须外挂。
3. **语义坍塌风险**：掩码与值在传输、缓存、算子融合中容易被分别处理，导致"未知"被静默降级为"空闲"——这正是安全关键系统中一类经典失效模式。

#### 2.3.3 失配三：有偏舍入（对应结构 C）

补码算术的**最简**舍入是向 $-\infty$ 截断：

$$Q_{2c}(x) = \lfloor x \rfloor \quad\Longrightarrow\quad \mathbb{E}[Q_{2c}(x) - x] = -\tfrac{1}{2}\ \text{ULP}$$

偏差恒为负、与数据分布无关，是一个**系统性偏置**。虽然可改用 round-half-to-even 消除，但其代价是额外的比较与修正逻辑，且在硬件截断路径上并非默认行为。

由 2.2.3 的交叉分析，该偏置在 $T \gtrsim 4$ 步后即成为长时程预测的主导误差源。

---

## 3 平衡三进制的结构性契合

平衡三进制使用数码集 $\{T, 0, 1\}$（即 $-1, 0, +1$），数值表示为

$$x = \sum_{i} d_i 3^i, \qquad d_i \in \{-1, 0, +1\}$$

其值域关于零点完全对称。一个 $n$ trit 的数可表示 $\frac{3^n - 1}{2}$ 至 $-\frac{3^n - 1}{2}$ 的全部整数，**无符号位、无两个零、无正负区间不对称**。

### 3.1 契合一：$\mathrm{sgn}$ 的数码化（对应结构 A）

**核心观察**：$\mathrm{sgn}$ 函数的码域 $\{-1, 0, +1\}$ **就是**平衡三进制的数码集。这不是效率优化上的巧合，而是结构同构。

由此得到一个直接结论：

> 在平衡三进制中，符号提取只需读取最高非零 trit。

形式化地，设 $x = \sum_{i=0}^{k} d_i 3^i$ 且 $d_k \neq 0$，则

$$\mathrm{sgn}(x) = d_k$$

该操作是 $O(1)$ 的单 trit 读取，**无需比较、无需分支、无需补码修正**。相比之下，二进制补码需要 $\Theta(n)$ 位检查加分支。

进一步，$\mathrm{sgn}$ 的三种输出在物理上对应三种不同的**接触状态**：

| $\mathrm{sgn}$ 输出 | 物理语义（摩擦） | 物理语义（控制） |
| --- | --- | --- |
| $-1$ | 负向滑动 | 反向饱和 |
| $0$ | **黏滞（stick）** | **死区 / 零输出** |
| $+1$ | 正向滑动 | 正向饱和 |

注意零点在此处不是"无信息"，而是**最重要的信息**——黏滞态是整个接触动力学的核心分支。在平衡三进制中它是一个一等数码；在二进制补码中它只是值域中一个普通点，需要额外的比较逻辑才能识别。

**附带的一致性**：三值权重网络的量化算子正是 $\mathrm{sgn}(\cdot)$ 加阈值门控（第十九章 19.1 节）。因此在三进制体系下，**控制器中的非光滑非线性与网络中的量化非线性是同一个算子**。这一统一性在二进制栈中不存在——前者用分支实现，后者用数值逼近实现。

#### 3.1.1 零偏置截断的对照

一个值得强调的细节：结构 A 所需的"符号"与结构 C 所需的"对称舍入"在平衡三进制中由**同一个性质**同时满足。因为在 $\{-1,0,+1\}$ 表示下，向零截断即舍入到最近，且关于零点对称：

$$\mathbb{E}[Q_{bt}(x) - x] = 0$$

即 $\mu = 0$。代回 2.2.3 的交叉公式，偏差项消失，累积误差退化为纯粹的随机游走 $\sigma\sqrt{T}$。

**这正是三进制对长时程世界模型的核心贡献：把 $O(T)$ 的漂移降为 $O(\sqrt{T})$ 的扩散。**

### 3.2 契合二：未知作为一等公民（对应结构 B）

平衡三进制与 **Kleene 强三值逻辑 $K_3$** 在两个层面上是同构的：把 $1$ 读作"真"、$T$ 读作"假"、$0$ 读作"未知"，则 $K_3$ 的真值表为

$$\neg 0 = 0, \qquad 1 \wedge 0 = 0, \qquad T \wedge 0 = T, \qquad 1 \vee 0 = 1, \qquad T \vee 0 = 0$$

**关键性质是闭合性**：$K_3$ 在 $\wedge$、$\vee$、$\neg$ 下封闭，且构成一个 De Morgan 代数。这意味着"未知"参与运算后仍得到合法值，无需外挂掩码规则。对照 2.3.2 中二进制方案的三项代价（位宽翻倍、代数不闭合、语义坍塌），三进制方案同时消除了三者：

| 维度 | 二进制 $(v, m)$ 对 | 平衡三进制单 trit |
| --- | --- | --- |
| 每单元位宽 | 2 bit（4 状态，1 个不可达） | $\log_2 3 \approx 1.585$ bit（3 状态，全部可达） |
| $\wedge / \vee$ 闭合性 | 需外挂掩码规则 | 天然闭合（Kleene 代数） |
| "未知"被静默降级风险 | 高（值与掩码可分离） | 无（单一符号，不可分离） |
| 与三支决策的对应 | 需构造 | 直接对应 accept/reject/defer |

三值逻辑闭合性带来的一项具体能力是**安全的保守回退**。设安全谓词为 $\phi$，我们希望策略的输出为三值：

```
+1 : 确认安全   -> 正常执行
 0 : 未知       -> 保守回退（减速 / 停止 / 请求人类接管）
-1 : 确认不安全 -> 拒绝执行 / 急停
```

在二进制方案中，"未知"与"安全"在数值上极易混淆（尤其是当掩码位在算子融合中被丢弃时）；在三进制方案中，二者是**不同的符号**，混淆需要显式的类型错误而非静默的数值退化。这是一个工程上可验证的鲁棒性差异，我们将其形式化为假设 H2（第 4.3 节）。

### 3.3 契合三：零中心的对称性（对应结构 C）

除 3.1.1 已讨论的零偏置舍入外，平衡三进制在值域对称性上还有两项性质对强化学习尤为重要。

**（a）无符号零。** IEEE 754 存在 $+0$ 与 $-0$ 两个零，且 $+0 = -0$ 为真、$\mathrm{sgn}(+0) = +0$、$\mathrm{sgn}(-0) = -0$，在分支密集的数值代码中是经典陷阱源。平衡三进制只有一个零。

**（b）值域关于零点对称。** 补码的 $n$ 位值域为 $[-2^{n-1}, 2^{n-1} - 1]$，正负不对称，导致对称截断（clipping）引入单侧偏置。平衡三进制的 $n$ trit 值域为 $\left[-\frac{3^n-1}{2}, +\frac{3^n-1}{2}\right]$，完全对称。

| 表示 | 位宽 | 状态数 | 值域 | 对称性 |
| --- | --- | --- | --- | --- |
| int8 补码 | 8 bit | 256 | $[-128, 127]$ | 否 |
| 5 trit 平衡三进制 | 7.925 bit | 243 | $[-121, 121]$ | **是** |

这一性质对强化学习的意义在于：**优势函数 $\hat{A}(s,a)$、时序差分误差 $\delta_t$、以及对称奖励 $\{-1, 0, +1\}$ 都在零点附近近似对称分布**。用非对称格式量化这些量，会系统性地压缩一侧、拉伸另一侧，相当于给价值估计注入了方向性偏差。而对称量化对这类量是无损的（在相同动态范围内）。

**（c）动作原语的天然三值性。** 机器人技能库中的大量离散动原本就是三值的：

| 平台 | 三值动作 |
| --- | --- |
| 差速驱动底盘 | 左转 / 直行 / 右转 |
| 平行夹爪 | 打开 / 保持 / 闭合 |
| 三指抓取的力控 | 松开 / 维持 / 加力 |
| 稀疏奖励任务 | $-1$ 失败 / $0$ 进行中 / $+1$ 成功 |

在这些场景中，三进制不需要"逼近"什么——它与任务本身共享离散结构。

### 3.4 契合四：事件驱动感知与硬件亲和

这一契合与前三条不同：前三条是表示层的，这一条是物理层的。

**（a）事件相机原生输出三值流。** 动态视觉传感器（DVS）对每个像素在亮度变化超过阈值时输出极性事件。其输出空间为

$$\{\text{ON}\} \cup \{\text{OFF}\} \cup \{\text{无事件}\} \;\cong\; \{+1, -1, 0\}$$

这是**字面意义上的平衡三进制流**，无需转码。事件相机同时具有高时间分辨率（微秒级）、高动态范围（>120 dB）和事件驱动的稀疏功耗，这三点恰好是具身感知（快速运动、强光/逆光、机载功耗受限）最需要的性质。

**（b）控制回路的时间稀疏性。** 具身控制通常以 100 Hz – 1 kHz 运行。在绝大多数时刻，绝大多数自由度应当输出零（无动作）。在平衡三进制中，$0$ 是一等符号，零值天然对应"跳过计算"；在二进制浮点中，零值同样特殊，但零稀疏性的产生依赖于习得的权重分布而非表示结构。

**（c）控制的滞环与死区。** 真实执行器存在死区与滞环（间隙、静摩擦）。在二进制中实现需额外的状态变量与比较逻辑；在三进制中，死区就是 $0$ 数码的带宽，滞环就是两个阈值之间的三值状态机。

**（d）神经形态硬件的事件驱动对应。** 脉冲神经网络（SNN）中兴奋性/抑制性突触与"无发放"恰好构成三值事件编码。神经形态芯片（如 Loihi 系列）的事件驱动、低功耗特性与机载机器人需求高度匹配。这意味着三进制的硬件优势在具身场景中**不必等待原生三进制 CMOS**，可以经由神经形态与存内计算路径先行兑现（详见第 5 节）。

### 3.5 契合总览

| 结构 | 具身建模难点 | 二进制失配 | 平衡三进制契合 | 可证伪假设 |
| --- | --- | --- | --- | --- |
| A 非光滑动力学 | 接触切换、摩擦、滑模 | 码域不同构；零点非一等值 | $\mathrm{sgn}$ 码域 $=$ 数码集；$0=$ 黏滞态 | H3 |
| B 部分可观测 | 遮挡、传感器失效、安全 | 认识态坍塌；代数不闭合 | Kleene $K_3$ 闭合；未知为一等值 | H2 |
| C 长时程一致性 | 世界模型多步 rollout | 截断有偏，$O(T)$ 漂移 | 零偏置舍入，$O(\sqrt{T})$ 扩散 | H1 |
| D 感知与执行 | 事件感知、稀疏动作 | 需转码或额外逻辑 | DVS 原生三值；零为一等值 | — |

> **注意 D 行的性质**：契合 D 是物理/工程层面的，不依赖任何学习算法。这意味着即使假设 H1–H3 全部被证伪，契合 D 依然成立并独立支持三进制在具身感知前端的使用。

---

## 4 可证伪假设与实验设计

### 4.1 方法论原则

第 3 节的论证若不落到可测量、可否证的命题上，就只是修辞。本节给出三个假设、配套实验，以及一个**关键的消融维度**。

**关键消融：三种执行模式。** 任何"三进制更优"的实验都必须区分以下三种模式，否则无法判断优势来源：

| 模式 | 描述 | 硬件 |
| --- | --- | --- |
| **C**（Continuous） | 全精度/连续值基线 | 二进制 |
| **A**（Quantized-Ternary） | 三值量化，模拟运行 | **二进制** |
| **B**（Native-Ternary） | 原生三值代数与运算 | 三值 / 仿真 |

三种结果的解读方式：

```
若 A ≈ B ≈ C : 三进制无表示层优势（仅可能有实现效率优势）-> 第 3 节论证被证伪
若 A ≈ C 且 B 显著优于 C : 优势来自表示代数本身       -> 强结论成立
若 A < C 且 B ≈ C : 三值量化带来损失，原生表示不损失   -> 弱结论成立（可无损压缩）
```

**这一消融表是本文最核心的实验设计。** 缺少它的实验结果无法支持或反驳本文主张。

### 4.2 H1：长时程 rollout 漂移

**假设 H1**：在相同比特预算下，采用对称三值量化的世界模型在长时程 rollout 中的累积预测误差增长阶显著低于采用非对称补码截断的量化模型。

**理论预测**（由 2.2.3 与 3.1.1）：

$$\text{非对称：}\ \mathbb{E}[E_T] \sim T\mu \quad\text{（线性）}, \qquad \text{对称：}\ \mathbb{E}[E_T] \sim \sigma\sqrt{T} \quad\text{（次线性）}$$

**实验设置**：

- **模型**：Dreamer 风格的隐空间世界模型，或扩散策略的动作序列预测器；
- **环境**：接触丰富的仿真任务（装配、推挤、多物体堆叠），因其 rollout 误差对数值精度最敏感；
- **对照**：固定总比特预算，对比 (i) 对称三值量化、(ii) 非对称截断量化、(iii) 对称二进制量化（round-half-to-even，作为排除"对称性"以外因素的对照）；
- **测量**：绘制 $\log\|\hat{s}_T - s_T\|$ vs $\log T$，拟合斜率。

**判据与证伪**：若三种方法的漂移曲线斜率无显著差异（统计检验 $p > 0.05$），H1 被证伪。若 (i) 显著优于 (ii) 且与 (iii) 相当，则说明优势来自**对称性**而非三进制特有的性质——这本身是一个有价值但**不同于本文**的结论，应如实报告。

> 第三种对照 (iii) 的设计意图是分离"三值"与"对称"两个因素。缺少它，实验无法排除"任何对称格式都行"的替代解释。

### 4.3 H2：未知态安全性

**假设 H2**：将"未知"作为一等值的三值策略，在遮挡与传感器失效场景下的安全违规率显著低于使用值-掩码对的二值策略，且任务成功率不劣化。

**实验设置**：

- **任务**：室内导航与抓取（含安全谓词："前方可通行"、"夹爪夹持牢固"）；
- **工况**：注入传感器遮挡、丢包、标定漂移；
- **对照**：(i) 三值策略（unknown 一等值）；(ii) 二值策略 + 显式掩码位；(iii) 二值策略 + 二值掩码（即把"未知"映射为"空闲"）；
- **测量**：安全违规率（碰撞、掉落）、保守回退触发率、任务成功率、以及**掩码降级率**（unknown 被静默解释为 free 的比例）。

**判据与证伪**：若 (i) 与 (ii) 的违规率无显著差异，则 H2 被证伪，说明"未知的一等值化"在统计上不带来安全收益。若 (iii) 与 (ii) 差异显著，则说明失效模式主要来自工程实现而非表示选择，本文对"语义坍塌风险"的强调应相应减弱。

**预期次要发现**：掩码降级率在算子融合、量化、缓存复用较多的实现中应显著升高。这是一项**与任务性能无关的可测量指标**，可作为表示鲁棒性的直接证据。

### 4.4 H3：接触任务的颤振

**假设 H3**：采用 $\mathrm{sgn}$ 原生三值控制器（含原生死区与滞环）的接触丰富任务，其控制颤振能量与接触力方差显著低于"连续控制器 + 人工附加死区"的基线。

**实验设置**：

- **任务**：插孔（peg-in-hole）、拧螺丝、抓取脆弱物体；
- **对照**：(i) 三值控制器（原生死区/滞环）；(ii) 连续滑模/阻抗控制器 + 人工死区；(iii) 连续控制器无死区；
- **测量**：控制信号的高频能量 $\int |\dot{u}|^2 dt$、接触力方差 $\mathrm{Var}[f_n]$、任务完成时间、执行器温升。

**判据与证伪**：若 (i) 与 (ii) 的颤振指标无显著差异，则 H3 被证伪——说明死区/滞环的收益是**功能性的**而非**表示性的**，三进制在此处没有独特优势。这是一个真实的可能性，应如实报告。

### 4.5 评价指标汇总

| 假设 | 主指标 | 次要指标 | 关键对照 |
| --- | --- | --- | --- |
| H1 | 漂移-步数曲线斜率 | 长时程任务回报 | 对称二进制量化 |
| H2 | 安全违规率 | 掩码降级率、回退率 | 二值掩码分离方案 |
| H3 | 颤振能量、力方差 | 执行器温升 | 人工死区方案 |

### 4.6 前置的可行性验证

在投入上述实验之前，建议先做三项低成本验证，用以快速排除最可能的证伪路径：

1. **漂移实验（纯数值，无需训练）**：对固定随机序列累积量化，直接比较对称与非对称舍入的 $E_T$ 增长阶。此实验可在数分钟内完成，若结果与 2.2.3 理论不符，则整个 H1 的理论基础需要修正。
2. **sgn 提取开销（微基准）**：在目标硬件上测量两种表示下符号提取的周期数与分支预测失败率。
3. **掩码降级审计（静态分析）**：在现有二值安全系统代码中统计"未知"被降级为"空闲"的路径数量，用于校准 H2 的现实相关性。

---

## 5 硬件实现路径

### 5.1 三时间尺度路线图

```
时间轴    ┌──────────────┬────────────────────┬──────────────────┐
         │   近期 0-2 年 │   中期 2-7 年        │   远期 7 年+     │
         ├──────────────┼────────────────────┼──────────────────┤
载体     │ GPU / NPU    │ 存内计算、神经形态   │ 原生三进制 ISA   │
         │ （二进制HW）  │ （模拟/事件驱动）    │ （三进制数据通路）│
         ├──────────────┼────────────────────┼──────────────────┤
兑现性质 │ 效率 + 表示   │ 效率 + 事件稀疏      │ 表示层完整兑现   │
         │ （部分）      │                     │                  │
         ├──────────────┼────────────────────┼──────────────────┤
适用契合 │ A(部分) B C D │ D（主） A B C（部分）│ A B C D（全部）  │
         ├──────────────┼────────────────────┼──────────────────┤
证据     │ BitNet b1.58 │ Loihi、忆阻器多值    │ Setun 血统       │
         │ TWN、TTQ     │ 事件相机量产         │ 尚无工程验证     │
         └──────────────┴────────────────────┴──────────────────┘
```

**路线图的关键含义**：本文的表示层论证（第 3 节）在**近期**即可通过模式 A（三值量化，运行于二进制硬件）部分检验；而契合 D（事件驱动感知）在**中期**由神经形态与事件相机独立兑现，**不依赖原生三进制硬件**。原生三进制 ISA 只在远期才成为完整兑现的必要条件。

### 5.2 近期（0–2 年）：二进制硬件上的三值量化

这一阶段的证据基础最为坚实。三值权重网络（TWN）、可训练三值量化（TTQ）与 BitNet b1.58 已证明三值量化在语言与视觉任务上可行，其中 BitNet b1.58 报告了与全精度基线持平的困惑度以及显著的延迟与能效收益。

对具身智能而言，近期的可操作路径是：

- 将 VLA（vision-language-action）模型的动作头（action head）与价值头（value head）三值化；
- 对优势函数、TD 误差使用**对称**量化（利用 3.3 节的对称性）；
- 将占据栅格、安全谓词改用单 trit 三值编码（利用 3.2 节的闭合性）；
- 保持路由层、安全层、本体感觉环为全精度。

**现实约束**：这一阶段兑现的是"效率 + 部分表示收益"，因为三值在二进制硬件上是**模拟**运行的，$\mathrm{sgn}$ 的 $O(1)$ 特性无法体现，Kleene 闭合性需要软件层维护。

### 5.3 中期（2–7 年）：存内计算与神经形态

三进制在此阶段的主要机会来自**新兴器件**而非主流 CMOS：

| 器件/架构 | 三值实现机制 | 具身适配性 |
| --- | --- | --- |
| 忆阻器 / RRAM | 多电导态（含中间态） | 存内计算，消除数据搬运 |
| CNTFET | 手性决定的阈值电压 | 多阈值、低静态功耗 |
| 神经形态芯片 | 事件驱动、三值事件编码 | 与 DVS 原生对接 |
| 模拟存内计算 | 电流求和、多值存储 | 高能效 MAC |

其中**事件相机 + 神经形态**的组合对具身智能的适配性最强：感知到执行的整条链路都是事件驱动的三值信号，无需转码，且功耗与数据量都与场景活动度成正比（静止场景近乎零功耗）。这是一条**不依赖原生三进制算术单元**的独立路径。

**必须承认的技术障碍**：多值 CMOS 逻辑的噪声容限显著低于二值 CMOS，标准单元库不成熟，EDA 工具链缺失。因此这一阶段的实际部署更可能发生在**模拟/事件驱动**而非**多值数字逻辑**方向。

### 5.4 远期（7 年+）：原生三进制 ISA

只有在原生三进制数据通路上，第 3 节的表示层论证才能完整兑现：$\mathrm{sgn}$ 成为单 trit 读取，Kleene 运算成为原生门，零偏置舍入成为硬件默认行为。

这一路径的技术血统可追溯至 Setun，但**必须诚实指出**：自 Setun 之后，原生三进制计算从未获得过现代工艺节点的工程验证。其成功取决于 EDA 工具链、标准单元库与形式验证方法是否成熟——这三项目前在第十七章中已被列为开放问题。因此本文将其定位为**远期可能性**，而非近期方案。

### 5.5 成本-收益概览

| 阶段 | 增量成本 | 主要收益 | 风险 |
| --- | --- | --- | --- |
| 近期 | 低（软件层） | 效率 + 部分表示收益 | 精度掉点需调参 |
| 中期 | 中（新器件） | 事件稀疏功耗、存内计算 | 器件良率、工具链 |
| 远期 | 高（全新栈） | 表示层完整收益 | 生态、成本、不确定性高 |

**结论**：三进制在具身智能中的价值兑现**不需要等待原生硬件**。近期的三值量化即可检验本文的表示层假设，而中期的神经形态路径有独立的工程价值。这一"渐进可兑现"性质，是具身智能方向相比"重造整个计算栈"的纯体系结构主张更务实的地方。

---

## 6 局限与反驳

### 6.1 四种主要质疑

**R1："三进制只是省电，不解决建模问题。"**

**回应**：这一批评对**效率叙事**成立，对本文不完全成立，但部分成立。本文明确承认三进制不解决数据稀缺、奖励设定与 sim-to-real 差距（2.1 节的边界划分）。本文的主张是：在**表示层**，三进制的数码集与 $\mathrm{sgn}$、Kleene 三值逻辑、对称舍入共享结构。这一主张要么可通过 H1–H3 检验，要么不成立。将其归约为"省电"是对第 3 节的误读。

**R2："二进制可以模拟三值，何必原生三进制？"**

**回应**：完全正确，且这正是本文设计 A/B/C 三模式消融（4.1 节）的原因。**若 A ≈ B，本文的强主张被证伪**。本文的态度是：这一问题的答案只能由实验给出，不能由论证给出。本文的贡献是把问题转化为可裁决的实验。

**R3："非光滑动力学可以用光滑近似（soft-sign、正则化接触模型）。"**

**回应**：部分成立。光滑近似确实广泛使用，但代价是：引入刚度（stiffness）导致仿真步长被迫缩小；黏滞态（$v = 0$）被抹平为一个区间而非一个确定状态，而黏滞/滑动的切换恰恰是接触动力学的核心。此外，真实物理本身是非光滑的，光滑化是近似而非等价。因此该质疑削弱但不消除契合 A 的相关性。

**R4："三值量化已有大量精度掉点的反例。"**

**回应**：成立，但不构成对本文的反驳，因为本文与量化文献的问题设定不同。量化文献关心"用更少比特逼近全精度"（数值逼近问题）；本文关心"哪种表示与问题结构同构"（表示选择问题）。一个量化方案精度掉点，恰恰可能说明它是在**错误的表示**上做逼近。二者可以独立成立。

### 6.2 本文不主张的结论

为免误读，明确列出：

- ❌ **三进制能提升样本效率**。本文未提供任何论证或证据支持此点。
- ❌ **三进制能解决 sim-to-real 差距**。该问题与数值表示基本正交。
- ❌ **原生三进制硬件近期可量产**。见 5.4 节的明确限定。
- ❌ **三进制在具身智能中必然优于二进制**。本文主张的是**结构性契合**与**可证伪假设**，不是已证实的性能优势。
- ❌ **本文的四个契合是完备的**。可能还存在未识别的契合或反向失配。

### 6.3 未解决的问题

1. **三值世界模型的训练理论**：三值约束下的表示学习泛化界尚不完善；STE 在长时程 rollout 下的梯度估计偏差缺乏分析。
2. **三值强化学习的收敛性**：在对称量化下，值迭代与策略梯度的收敛条件是否保持？
3. **契合之间的相互作用**：四个契合是否可叠加？是否存在此消彼长（例如三值稀疏性与量化精度损失）？
4. **基准缺失**：目前没有面向"表示层亲和性"评测的公开基准，H1–H3 都需要自建环境与协议。
5. **硬件噪声容限**：多值逻辑在先进工艺下的实际噪声容限尚未有系统性的公开数据。

---

## 7 结论

本文论证了三进制在具身智能中的优势**在性质上不同于**其在语言模型中的优势。

在语言模型领域，三进制的既有论证是效率叙事：三值量化消除乘法、降低带宽，最终体现为 BitNet b1.58 的能效收益。这一叙事的本质是**用更少的资源做同样的事**。

在具身智能领域，本文论证了另一类关系——**表示层的结构同构**：

1. $\mathrm{sgn}$ 函数的码域 $\{-1, 0, +1\}$ **就是**平衡三进制的数码集。接触动力学、库仑摩擦、滑模控制、继电器与死区的数学核心正是 $\mathrm{sgn}$，这是具身智能最难方向（接触丰富操作）的本质困难所在。
2. **Kleene 三值逻辑 $K_3$ 在 $\wedge, \vee, \neg$ 下闭合**，使"未知"成为一等公民并保持代数封闭。这与 POMDP 的认识不确定性、安全谓词的三态语义、以及工程实践中被反复重新发明的三值占据栅格（ROS/Nav2/OctoMap）直接对应。
3. **零偏置的对称舍入**把长时程 rollout 的累积误差从 $O(T)$ 的系统性漂移降为 $O(\sqrt{T})$ 的随机扩散。定量地，有偏项在 $T \gtrsim (\sigma/\mu)^2$ 步后即主导——以典型 ULP 尺度估算仅约 4 步，而具身世界模型的 rollout 通常是 $10$–$10^3$ 步。
4. **事件相机的原生输出就是三值流**（$+1/-1$ 极性加零事件），神经形态硬件的事件驱动特性与之自然对接。这一契合是物理层的，**不依赖任何学习算法，也不依赖原生三进制硬件**。

本文的立场是克制的：不主张三进制能提升样本效率、能弥合 sim-to-real 差距、或必然优于二进制。本文主张的是，具身智能中存在三类对符号高度敏感的建模结构，而平衡三进制的数码集与这三类结构同构——并且这一主张已被转化为三个可证伪假设与一套含关键消融的实验设计。

如果这一主张成立，其意义在于重新定位三进制的价值：**它不是一个更省钱的数字系统，而是一个与具身智能的数学结构更贴合的数字系统。** 语言模型的三进制化是工程优化，具身智能的三进制化可能是一次表示层的重新对齐。

反之，如果 H1–H3 被证伪，那么契合 D（事件驱动感知）仍将独立成立，而本文的其余论证应被明确撤回。**这正是本文希望交付的东西：一组可以被推翻的具体命题，而不是一篇无法被证伪的宣言。**

---

## 参考文献

### A 三进制与多值逻辑

1. Brusentsov, N. P. et al. "Small Electronic Computer Setun." *Moscow State University*, 1959.
2. Hurst, S. L. "Multiple-Valued Logic — Its Status and Its Future." *IEEE Transactions on Computers*, C-33(12), 1984.
3. Smith, K. C. "Multiple-Valued Logic: A Tutorial and Appreciation." *Computer*, 21(4), 1988.
4. Miller, D. M. & Thornton, M. A. "Multiple Valued Logic: Concepts and Representations." *Morgan & Claypool*, 2007.
5. Post, E. L. "Introduction to a General Theory of Elementary Propositions." *American Journal of Mathematics*, 43(3), 1921.

### B 三值量化与神经网络

6. Li, F., Zhang, B. & Liu, B. "Ternary Weight Networks." *arXiv:1605.04711*, 2016.
7. Zhu, C. et al. "Trained Ternary Quantization." *ICLR*, 2017.
8. Wang, H. et al. "BitNet: Scaling 1-bit Transformers for Large Language Models." *arXiv:2310.11453*, 2023.
9. Ma, S. et al. "The Era of 1-bit LLMs: All Large Language Models are in 1.58 Bits." *arXiv:2402.17764*, 2024.

### C 具身智能与世界模型

10. Kaelbling, L. P., Littman, M. L. & Cassandra, A. R. "Planning and Acting in Partially Observable Stochastic Domains." *Artificial Intelligence*, 101(1–2), 1998.
11. Levine, S. et al. "End-to-End Training of Deep Visuomotor Policies." *JMLR*, 17(39), 2016.
12. Tobin, J. et al. "Domain Randomization for Transferring Deep Neural Networks from Simulation to the Real World." *IROS*, 2017.
13. Ha, D. & Schmidhuber, J. "World Models." *NeurIPS*, 2018.
14. Hafner, D. et al. "Dream to Control: Learning Behaviors by Latent Imagination." *ICLR*, 2020.
15. Chi, C. et al. "Diffusion Policy: Visuomotor Policy Learning via Action Diffusion." *RSS*, 2023.
16. Black, K. et al. "π₀: A Vision-Language-Action Flow Model for General Robot Control." *arXiv:2410.24164*, 2024.

### D 非光滑动力学与控制

17. Stewart, D. E. & Trinkle, J. C. "An Implicit Time-Stepping Scheme for Rigid Body Dynamics with Inelastic Collisions and Coulomb Friction." *IJNME*, 39(15), 1996.
18. Anitescu, M. & Potra, F. A. "Formulating Dynamic Multi-Rigid-Body Contact Problems with Friction as Solvable Linear Complementarity Problems." *Nonlinear Dynamics*, 14, 1997.
19. Utkin, V. I. "Sliding Modes in Control and Optimization." *Springer*, 1992.
20. Slotine, J.-J. & Li, W. "Applied Nonlinear Control." *Prentice Hall*, 1991.

### E 三值逻辑与三支决策

21. Kleene, S. C. "Introduction to Metamathematics." *North-Holland*, 1952.
22. Yao, Y. "Three-Way Decisions with Probabilistic Rough Sets." *Information Sciences*, 180(3), 2010.

### F 事件感知、神经形态与新兴器件

23. Lichtsteiner, P. et al. "A 128×128 120 dB 15 μs Latency Asynchronous Temporal Contrast Vision Sensor." *IEEE JSSC*, 43(2), 2008.
24. Gallego, G. et al. "Event-Based Vision: A Survey." *IEEE TPAMI*, 44(1), 2022.
25. Davies, M. et al. "Loihi: A Neuromorphic Manycore Processor with On-Chip Learning." *IEEE Micro*, 38(1), 2018.
26. Strukov, D. B. et al. "The Missing Memristor Found." *Nature*, 453, 2008.
27. Lakshmanan, Y. et al. "CNTFET-Based Ternary Logic Circuits." *IEEE Transactions on Nanotechnology*, 2016.
28. Sasamal, T. N. et al. "Ultra-Low Power CNTFET-Based Ternary Logic Circuits." *IET Computers & Digital Techniques*, 2017.
29. Jouppi, N. P. et al. "Ten Lessons from Three Generations of TPU." *ISCA*, 2021.

---

## 附录：核心命题的形式化摘要

**命题 1（$\mathrm{sgn}$ 同构）**
设 $x = \sum_{i=0}^{k} d_i 3^i$ 为平衡三进制表示且 $d_k \neq 0$，则 $\mathrm{sgn}(x) = d_k$。符号提取为 $O(1)$ 单 trit 读取。

**命题 2（Kleene 闭合）**
数码集 $\{-1, 0, +1\}$ 上的 Kleene 强三值逻辑 $K_3$ 在 $\neg, \wedge, \vee$ 下封闭，构成 De Morgan 代数。其中 $0$ 解为"未知"时，运算结果保持合法，无需外挂掩码。

**命题 3（零偏置舍入）**
平衡三进制的向零截断满足 $\mathbb{E}[Q_{bt}(x) - x] = 0$；补码向 $-\infty$ 截断满足 $\mathbb{E}[Q_{2c}(x) - x] = -\frac{1}{2}$ ULP。

**命题 4（漂移交替点）**
设每步量化误差独立同分布，均值 $\mu$、标准差 $\sigma$，则累积误差的偏差项主导条件为 $T \gtrsim (\sigma / \mu)^2$。当 $\sigma \approx 1$ ULP、$\mu \approx 0.5$ ULP 时，$T \gtrsim 4$。

**命题 5（值域对称）**
$n$ trit 平衡三进制的值域为 $\left[-\frac{3^n-1}{2}, +\frac{3^n-1}{2}\right]$，关于零点完全对称；$n$ 位补码值域为 $[-2^{n-1}, 2^{n-1}-1]$，不对称。

---

## 附录：可复现的最小验证脚本

以下脚本用项目自带的 `BalancedTernary` 类验证命题 3 与命题 4，无需训练即可完成漂移实验（对应 4.6 节的前置验证 1）。

```python
"""
最小验证：对称 vs 非对称舍入的累积漂移增长阶
依赖：本仓库 balanced_ternary_calculator.py
"""
import random
import math
from balanced_ternary_calculator import BT

def accumulation_curve(rounder, n_steps=512, trials=2000, seed=0):
    """返回 [E[|累积误差|] for T in 1..n_steps]，误差以 ULP 为单位。"""
    rng = random.Random(seed)
    curves = [0.0] * n_steps
    for _ in range(trials):
        acc = 0.0
        for t in range(n_steps):
            x = rng.uniform(-1.0, 1.0)      # 归一化到 ±1 ULP 量级
            acc += rounder(x) - x           # 单步量化误差
            curves[t] += abs(acc)
    return [c / trials for c in curves]

def trunc_toward_neg_inf(x):
    """补码风格：向 -inf 截断（有偏，mu = -0.5 ULP）"""
    return math.floor(x)

def trunc_toward_zero(x):
    """平衡三进制风格：向零截断（对称，mu = 0）"""
    return math.trunc(x)

def fit_growth_exponent(curve, lo=64, hi=512):
    """对 log(误差) vs log(T) 做最小二乘，返回增长指数。"""
    xs = [math.log(t) for t in range(lo, hi)]
    ys = [math.log(max(curve[t - 1], 1e-12)) for t in range(lo, hi)]
    n = len(xs)
    mx, my = sum(xs) / n, sum(ys) / n
    num = sum((x - mx) * (y - my) for x, y in zip(xs, ys))
    den = sum((x - mx) ** 2 for x in xs)
    return num / den

if __name__ == "__main__":
    c_biased   = accumulation_curve(trunc_toward_neg_inf)
    c_symmetric = accumulation_curve(trunc_toward_zero)

    T_MAX = 512
    sigma_sym = 1.0 / math.sqrt(3.0)   # 向零截断残差的 sigma（x~U(-1,1)）
    sigma_bias = 1.0 / math.sqrt(12.0)  # 向 -inf 截断残差的 sigma

    print(f"非对称截断  T={T_MAX} 累积误差 = {c_biased[-1]:.3f} ULP")
    print(f"对称截断    T={T_MAX} 累积误差 = {c_symmetric[-1]:.3f} ULP")
    print()
    print("理论预测：")
    print(f"  非对称（有偏线性）  T*|mu|            = {T_MAX * 0.5:.1f} ULP")
    print(f"  对称（随机游走）    sigma*sqrt(2T/pi) = "
          f"{sigma_sym * math.sqrt(2 * T_MAX / math.pi):.3f} ULP")
    print()
    print(f"非对称截断 增长指数 gamma = {fit_growth_exponent(c_biased):.3f}  (理论 ~1.0)")
    print(f"对称截断   增长指数 gamma = {fit_growth_exponent(c_symmetric):.3f}  (理论 ~0.5)")

    # 平衡三进制往返一致性（命题 3、命题 5）
    # 注意：BT(x) 返回 BalancedTernary；数值取值用 int()（to_decimal() 返回字符串）
    assert int(BT('1T')) == 2,                        "1T = 3 - 1 = 2"
    assert int(BT('T')) == -1,                        "T = -1（无符号位，天然负数）"
    assert int(BT('10T')) == 8,                       "10T = 9 + 0 - 1 = 8"
    assert int(BT('1TTTT')) == 81 - 27 - 9 - 3 - 1,   "5 trit 上界对称性抽样"
    for n in range(-13, 14):
        assert int(BT(n)) == n, f"round-trip failed at {n}"
    print("\n平衡三进制往返一致性检查通过（n in [-13, 13]）")
```

**预期输出**：非对称截断的增长指数接近 $1.0$，对称截断接近 $0.5$。若实际结果偏离，则命题 4 的适用条件（误差独立性、$\sigma/\mu$ 量级）需要重新审视，第 4.2 节的 H1 实验设计应相应修正。

---

<div align="center">

**本文档为独立论文，与 `ternary-computing.md`（三进制计算：从入门到研究）配套阅读。**

相关章节：第十八章（AI 硬件加速）、第十九章（AI 深度学习）、第十七章（开放问题）

</div>
